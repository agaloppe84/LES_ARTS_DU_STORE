<%= simple_form_for [@product, @info] do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.input :icon, as: :attachinary %>
  <%= f.submit "Rajouter une info", class: "btn btn-primary" %>
<% end %>

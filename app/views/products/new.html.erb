<%= simple_form_for(@product) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :photos, as: :attachinary %>
  <%= f.association :type, as: :radio_buttons %>
  <%= f.submit %>
<% end %>

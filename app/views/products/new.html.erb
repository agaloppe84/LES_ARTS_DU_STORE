<%= simple_form_for(@product) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :photos %>
  <%= f.association :type, as: :radio_buttons %>

  <%= f.input :photos_cache, as: :hidden %>
  <%= f.submit %>
<% end %>
